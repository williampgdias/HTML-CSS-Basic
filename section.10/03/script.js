const text = 'Javascript';
const frase = 'Isso é o jogo';

console.log(text + ' ' + frase);

let total = 0;

const compras = 30;
const imposto = 10;

total = compras + imposto;

const dobro = total * 2;
const dividir = total / 2;
const desconto = total - 20;

console.log(dobro, dividir, desconto);
